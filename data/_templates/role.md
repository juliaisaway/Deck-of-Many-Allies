---
id: role-id                 # required, kebab-case, must match filename
name: Role Name             # required, localized display name

author: your-name           # optional, contributor credit
---

# Role Name

> Add a short description only if this role entry needs one in your workflow. The current build uses role metadata, so body text is optional.
